@* Para convertirlo en fuertemente tipado e indicarle que únicamente permita objetos derivados del modelo Escuela  *@

@model Asignatura
@* Declarando código Razor en una sola línea , se inicia la línea con @*@
@* Declarando código Razor en más de una línea con @ + {} *@

@{
    ViewData["Title"] = "Información de Asignatura";
    Layout = "Simple"; //Se le indica que utlice una plantilla situada en la carpeta Shared
}


<h1> @ViewData["Title"] </h1>

<strong> Desde la vista normal </strong>
<p>
    Nombre: @Model.Nombre <br />
    Id: @Model.UniqueId
</p>

<strong> Desde la vista parcial</strong>
@* "partial": Elemento que ya traer ASP.Net Core que se utiliza como tag helper para extender el html desde el lado del servidor.*@
@*Por convencion, se asume que si no pasamos el modelo entonces, se utiiza el modelo ya contemplado en la vista actual *@
@* Otro parametro puede ser "model" *@
<partial name="AsignaturaSimple" />


@*Acceder al ViewBag*@
<p><strong><i> Fecha Actual: @ViewBag.FechaActual </i></strong></p>
@*Acceder con ViewData*@
<p><strong><i> Fecha Actual: @ViewData["FechaActual"] </i></strong></p>